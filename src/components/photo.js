import React from 'react';
import '../index.css'

let Photo = (props) => {
  //let url = `https://${props.data.farm}.staticflickr.com/${props.data.server}/${props.data.id}_${props.data.secret}.jpg`
  return(
    <li>
      <img alt=''/>
    </li>
  )
}

export default Photo
